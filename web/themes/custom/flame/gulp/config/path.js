const buildFolder = `../flame/dist`
const srcFolder = `../flame`

export const path = {
  build: {
    css: `${buildFolder}/css`
  },
  src: {
    scss: `${srcFolder}/assets/sass/style.scss`
  },
  watch: {
    scss: `${srcFolder}/assets/sass/**/*.scss`
  }
}
